server:
  port: 80

spring:
  application:
    name: sword-laboratory-eureka-application-service

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    preferIpAddress: true
#    hostname: ${HOST_NAME}